name: Daily GitHub Trending Report

on:
  schedule:
    # 北京时间每天 09:13 = UTC 01:13
    - cron: "13 1 * * *"
  workflow_dispatch:

jobs:
  run-report:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt

      - name: Run report task
        env:
          GITHUB_TRENDING_URL: ${{ secrets.GITHUB_TRENDING_URL }}
          GITHUB_TRENDING_SINCE: ${{ secrets.GITHUB_TRENDING_SINCE }}
          GITHUB_TRENDING_LIMIT: ${{ secrets.GITHUB_TRENDING_LIMIT }}
          DEEPSEEK_API_KEY: ${{ secrets.DEEPSEEK_API_KEY }}
          DEEPSEEK_BASE_URL: ${{ secrets.DEEPSEEK_BASE_URL }}
          DEEPSEEK_MODEL: ${{ secrets.DEEPSEEK_MODEL }}
          DEEPSEEK_TIMEOUT: ${{ secrets.DEEPSEEK_TIMEOUT }}
          FEISHU_WEBHOOK_URL: ${{ secrets.FEISHU_WEBHOOK_URL }}
          FEISHU_TIMEOUT: ${{ secrets.FEISHU_TIMEOUT }}
        run: python -m src.main
